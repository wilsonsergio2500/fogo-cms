# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          node-version: "14.x"
      - run: npm install
      - name: prepare enviroment
        run: |
          echo $DEV_ENV_FILE >> src/environments/environment.prod.ts
        shell: bash
        env:
          DEV_ENV_FILE : ${{secrets.NG_ENV_PROD}}
      - name: prepare enviroment Prod
        run: |
          echo $DEV_ENV_FILE >> src/environments/environment.ts
        shell: bash
        env:
          DEV_ENV_FILE : ${{secrets.NG_ENV_TEST}}
      - run: npm run build-prod
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FOGO_CMS }}'
          channelId: live
          projectId: fogo-cms
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
